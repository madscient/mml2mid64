# mml2mid

MML（Music Macro Language）で書かれたテキストファイルを読み込み、標準MIDIファイル
（SMF）を出力するコンパイラです。

## ⚠ 本リポジトリは非公式フォークです

**本リポジトリは、オリジナルの mml2mid Version 5.30b の非公式フォークです。
オリジナルの作者陣とは一切関係がなく、作者陣による公認・承認・サポートを受けた
ものではありません。**

- 本フォークに関する問い合わせ・不具合報告を、**オリジナルの作者陣に送らないで
  ください。** 本フォークで生じた問題の責任は、フォーク作成者(madscient)にあります。
- オリジナルの著作権は作者4氏に帰属したままです。本フォークによって著作権の帰属や
  利用条件が変わることはありません。
- オリジナルのライセンスは、再配布（「転載可」）と無保証を定めるのみで、**改変版の
  再配布を明示的に許諾していません。** 本フォークは執筆時点でその許諾を得られて
  おらず、オリジナル作者の意向により公開を取り下げる可能性があります。
  詳細は「[著作権とライセンス](#著作権とライセンス)」を参照してください。

| 項目 | 内容 |
| --- | --- |
| フォーク元 | mml2mid Version 5.30b |
| オリジナル作者 | 門田暁人、藤井秀樹（MKR）、黒田久泰、新出尚之 |
| 入手元 | <https://www.vector.co.jp/soft/dl/unix/art/se102432.html> |
| オリジナルのWebページ | `http://hpc.jp/~mml2mid/`（現在はアクセス不可） |
| 本フォークの位置づけ | **非公式・無認可**。64bit環境対応が目的 |

## 本フォークの内容

オリジナルのソースを、現代の64bit環境（Windows / Linux / macOS）でビルドできる
ようにモダナイズしたものです。**既存のMMLは書き直さずにそのまま使えます**
（[MMLの拡張](#mmlの拡張)は書き足せるようになっただけで、従来の書き方の解釈は
変わりません。生成MIDIが変わりうる変更が2件だけあります。不具合修正1件と仕様の
意図的な変更1件で、いずれも
[doc/CHANGES.md](doc/CHANGES.md#いちばん大事なこと) に記載しています）。

オリジナル配布物のうち、ドキュメント（`org-doc/`）、プリプロセッサ（`mmlpp/`）、
GUIフロントエンド（`tk/`）、および配布物の説明（[readme.txt](org-doc/readme.txt)）は
同梱しています。

**サンプル曲（`sample/`）は同梱していません。** 各曲の著作権はそれぞれの作曲者に
帰属する第三者の著作物であり、自由利用が明示されているのは門田氏作の曲のみのため、
本フォークでは再配布していません。回帰テストで使用する場合は、
[オリジナル配布物](https://www.vector.co.jp/soft/dl/unix/art/se102432.html)から
`sample/` をこのツリーの直下にコピーしてください（[test/README.md](test/README.md)）。

本フォークでの変更点は、次の3つに分けて記載しています。

| ドキュメント | 内容 |
| --- | --- |
| [doc/CHANGES.md](doc/CHANGES.md) | **オリジナル版との差分**（エンドユーザー向け。MMLの拡張など） |
| [doc/STATUS.md](doc/STATUS.md) | 変更履歴（開発側の作業記録） |
| [BUILD.md](BUILD.md) | ビルド方法とモダナイズの技術的詳細 |

概要は「[モダナイズの概要](#モダナイズの概要)」にもあります。

## クイックスタート

```sh
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build
```

`sample.mml` として下記の1行だけのファイルを用意し、

```
A C1 cdefg
```

コンパイルすると `sample.mid` ができ、「ドレミファソ」と鳴ります。

```sh
mml2mid sample.mml
```

先頭の `A` がトラック名、`C1` がMIDIチャンネル1の指定、続く `cdefg` が音符です。
MMLの詳しい書き方は [org-doc/mml2mid.txt](org-doc/mml2mid.txt)、コマンド一覧は
[org-doc/command.txt](org-doc/command.txt) を参照してください。これらはオリジナル版
時点の内容なので、本フォークで拡張した箇所は [doc/CHANGES.md](doc/CHANGES.md) で
読み替えてください。

## ビルド

プラットフォームは自動判別されるため、設定用のスイッチは不要です。

### CMake（全プラットフォーム、推奨）

```sh
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build
```

Windows の Visual Studio ではマルチコンフィグ構成になります。

```powershell
cmake -S . -B build -A x64
cmake --build build --config Release
```

インストールは `cmake --install build --prefix /usr/local` です。

### Makefile（Linux / macOS / *BSD / MinGW）

```sh
cd src
make
make check          # トラック名テストと、全サンプルのハッシュ照合
sudo make install   # 既定の PREFIX は /usr/local
```

### 必要なもの

- C11コンパイラ。既定では `gnu11` を選択します。本プログラムはPOSIX
  （`open` / `read` / `isatty` / `strdup`）を使用しており、厳密な `-std=c11`
  ではglibcがこれらの宣言を隠してしまうためです。`src/compat.h` 自身も
  feature-test マクロを設定しているので、厳密なC11でもビルドできます。
- 外部ライブラリへの依存はありません。

### 動作確認済みのツールチェーン

| ツールチェーン | プラットフォーム | 結果 |
| --- | --- | --- |
| MSVC 19.51（VS 18） | Windows x64 | `/W3` で警告ゼロ、サンプル回帰テスト 33/33 合格 |
| GCC 10 | Debian x86-64 | `-O2 -Wall -Wextra` で警告ゼロ、サンプル回帰テスト 33/33 合格 |
| clang 21（clang-cl） | Windows x64 | エラー・実質的な警告なし |

上記2つのフルビルドは、決定的な32サンプル全てでバイト単位で同一のMIDIを出力します。

## テスト

テストは3種類あります。

**サンプル曲の回帰テスト。** `sample/*.mml` をすべてコンパイルし、
`test/baseline.sha256` に記録されたSHA-256と照合します。生成MIDIが変化する変更は
即座に検出されます。**サンプル曲は同梱していないため、既定ではこちらは無効です**
（ビルド自体は正常に行えます。CMake がその旨のメッセージを表示します）。有効に
するには、[オリジナル配布物](https://www.vector.co.jp/soft/dl/unix/art/se102432.html)の
`sample/` ディレクトリをこのツリーの直下にコピーしてください。

**トラック名のテスト。** `test/trackname/*.mml` をコンパイルし、出力されたトラック
の一覧を隣の `.trk` ファイルと照合します。並列表記・ワイルドカード・2文字の
トラック名の解釈を固定するものです。本フォークで書いたMMLなので、こちらは常に
実行されます。

**不具合修正と新記法のテスト。** `test/regress/*.mml` をコンパイルし、
`test/regress.sha256` のSHA-256と照合します。オリジナル版のTODOリスト
（[org-doc/todo.txt](org-doc/todo.txt)）にあった不具合の修正と、その修正で
**変わってはいけない**ケース、および本フォークで追加したMMLの記法（長い名前の／
引数つきマクロ、`i`/`j` によるベンド値算出）を固定します。これも本フォークで
書いたMMLなので常に実行されます。

```sh
cd build && ctest            # CMake 3.20 未満
ctest --test-dir build       # CMake 3.20 以降
```

```powershell
ctest --test-dir build -C Release   # Windows / Visual Studio
```

このベースラインは自己参照ではなく、オリジナル配布物に同梱されていた `.mid`
（オリジナル版が生成したもの）と照合して検証済みです。挙動がオリジナル版と同一
だったモダナイズ時点で、**32件中28件がバイト単位で完全一致**しました。一致しない
4件の原因と根拠は [test/README.md](test/README.md) に記載しています（うち3件は
`.mid` 生成時のテンポ値の丸め方の違い、1件は乱数ベロシティによる非決定性で、
いずれも本リポジトリでの退行ではありません）。

その後、本フォークが**意図して入れた挙動変更**によって4件（`02nm63` は従属トラック
取りこぼしの修正、`01mkr29`／`01mkr39`／`01mkr40` は `M`コマンドと音長0の和音の
修正）の出力が変わったため、現在の一致数は25件です。内訳と追跡の根拠は
[test/README.md](test/README.md) にあります。

## ディレクトリ構成

| パス | 内容 |
| --- | --- |
| `src/` | ソースプログラムと Makefile |
| `org-doc/` | **オリジナルのドキュメント類**（MMLリファレンス、テクニック集など。内容は変更していません） |
| `doc/` | 本フォークのドキュメント（[CHANGES.md](doc/CHANGES.md)、[STATUS.md](doc/STATUS.md)） |
| `test/` | 回帰テスト用のベースライン・トラック名テスト・スクリプト（サンプル曲は非同梱） |
| `cmake/` | ctest から呼ばれるテスト実行スクリプト |
| `mmlpp/` | mml2mid用プリプロセッサ（Perlスクリプト）。**本フォークでは機能を本体に取り込んだので通常は不要です**（[doc/CHANGES.md](doc/CHANGES.md#外部プリプロセッサ-mmlpp-について)） |
| `tk/` | Tcl/Tk製のGUIフロントエンド tkmml2mid |

## モダナイズの概要

詳細および修正した不具合の一覧は [BUILD.md](BUILD.md)、時系列の記録は
[doc/STATUS.md](doc/STATUS.md) にあります。主な点は次の通りです。

### ビルドシステム

- `CMakeLists.txt`（MSVC / GCC / Clang対応）を追加し、`src/makefile` を
  `install` / `check` ターゲットを持つ移植性のあるMakefileに書き直しました。
- `-DUNIX` を廃止し、`_WIN32` の有無でプラットフォームを判別します。
- 旧来の `makefile.bcc` / `makefile.egc` / `makefile.lcc` および `file.asm`
  （16bit DOS用アセンブラ）は、どのビルドからも参照されなくなりました。
  歴史的資料として残してありますが、削除して構いません。

### 64bit対応

- `Fpos_t` が `long` でした。ファイル位置はポインタ同士の差から計算されるため、
  `long` が32bitであるWin64では、算出された位置が毎回切り詰められていました。
  現在は `ptrdiff_t` です。
- `qsort()` にプロトタイプなし関数ポインタ型へキャストした比較関数を渡していました
  （未定義動作であり、新しい規格では受け付けられません）。

### 移植性レイヤ

- `win.h` を `compat.h` に置き換え、旧来の `UNIX` / `WINDOWS` / `MSDOS` /
  `BCC` / `LSI_C` を手動で切り替える構成から、Win32とPOSIXの実際の分岐に整理しました。
  feature-testマクロを設定するため、各 `.c` ファイルの**最初の** `#include`
  である必要があります。
- DOS用ファーポインタ（LSI-C）版とBorland C版のメモリ上ファイル層、および
  Win16 GUI用のエントリポイント（`mml_smf`、`setjmp`/`longjmp` によるエラー処理、
  `hWnd3`、`InvalidateRect`/`UpdateWindow` のダミー）を削除しました。
- `isascii()`（標準Cではなく、MSVCには存在しない）を、`EOF` を正しく偽と判定する
  明示的な範囲チェックに置き換えました。

### 修正した不具合

モダナイズの過程で見つかった実際のバグです（詳細は [BUILD.md](BUILD.md)）。

- **`EX` / `EE` でのスタックバッファオーバーフロー** — `getexclusive()` が
  `int exclusive[1024]` に境界チェックなしで書き込んでいました。
- **乱数ベロシティでのゼロ除算** — `kr1`（または `kr n,0`）で除数が0になりました。
- **`text[]` のオーバーフロー** — 8KB固定バッファへ `strcat` で追記しており、
  `S` コマンドの多いトラックで溢れました。
- **タイトル/著作権表示のoff-by-one** — 長さは1バイトで書き出されるのに256を許して
  おり、長さ0として書き出されていました。
- `<errno.h>` が未インクルードのため `EINTR` の再試行が無効化されていた問題、
  パイプからの読み込みが空になる問題なども修正しました。
- **同じ行で主トラックより後ろに書いた従属トラックが捨てられていた問題**を修正
  しました。`A1A ...` と書くと `1A` が無視されていたものです（`1AA ...` なら
  正しく動いていました）。**生成MIDIが変わりうる修正**です。

### オリジナル版のTODOリストにあった不具合の修正

オリジナル作者が [org-doc/todo.txt](org-doc/todo.txt) に「以下のバグを直す．」
として挙げていた項目のうち3件を修正しました。いずれも**その不具合を踏んでいた
MMLの出力だけ**が変わります。詳細は
[doc/CHANGES.md](doc/CHANGES.md#不具合修正によって変わる挙動) を参照してください。

- **`M1,50 c0e2` のようにMコマンドと音長0の和音を併用してもモジュレーションが
  かかるようになりました。**
- **`=1`〜`=0` で飛ばした区間のテンポが、再開後のテンポを上書きしなくなりました。**
- **行の中に `=1` を書いたトラックが、後続のトラックに影響しなくなりました。**

### MMLの拡張

既存のMMLの解釈は変えないことを原則としつつ、コマンドをいくつか追加・拡張して
います。追加したコマンドの一覧と詳しい仕様は
**[doc/CHANGES.md](doc/CHANGES.md#mmlの拡張)** を参照してください（このREADMEには
一覧を置きません。二重管理になり、片方が古くなるためです）。

同梱の外部プリプロセッサ **mmlpp**（`mmlpp/mmlpp.pl`）が持っていた機能——長い名前の
マクロ `${名前}`、引数つきマクロ `${名前:引数}`、半音数からピッチベンド値を算出する
`i`/`j`——も本体に取り込みました。**mmlpp 向けに書かれたMMLは、`perl mmlpp.pl` を
通さずにそのままコンパイルできます。**

## 文字コードについて

オリジナルの配布物は全体が **EUC-JP** でした。本フォークでは、ソースとドキュメントを
**UTF-8** に変換し、コンパイラの入力データは元のまま残しています。

| 対象 | 文字コード | 備考 |
| --- | --- | --- |
| `src/` のソース（`.c` / `.h`） | UTF-8 | 非ASCII文字はすべて日本語コメント内。プログラムの文字列は不変 |
| ドキュメント（`org-doc/*.txt`、`doc/*.md`、`src/mml2mid.1`） | UTF-8 | 内容は1文字も変えずに変換済み |
| `org-doc/tr-rack.mml` | **EUC-JP のまま** | 下記の理由により変換しません |
| `org-doc/mml2mid.def` | 変更なし | VZエディタ用のバイナリを含む定義ファイル |
| `mmlpp/mmlpp.pl`、`tk/tkmml2mid.tcl` | EUC-JP のまま | ドキュメントではなくプログラム |

### `.mml` を変換しない理由

`.mml` はドキュメントではなく**コンパイラへの入力データ**です。mml2mid は文字列中の
バイトをそのままSMFのメタイベント（曲名・トラック名など）へ書き出すため、`.mml` の
文字コードを変えると**生成されるMIDIファイルの中身が変わります**。回帰テスト用に
オリジナルの `sample/` を持ち込む場合も、EUC-JP のまま無改変で置いてください。
変換するとベースラインと一致しなくなります。

`.mml` 内の日本語を扱う場合は、ソースの文字コードに応じて `-m` オプション
（文字列中をShift-JISとみなす）を使い分けてください。

### ビルド時の注意

ビルドファイルはMSVCに `/utf-8`、GCC/Clangに `-finput-charset=UTF-8` を渡します。
`cl` を手動で実行する場合は `/utf-8` を付けてください（付けないとC4819警告が出ます）。

## 著作権とライセンス

**本ソフトウェアの著作権は、オリジナルの作者である 門田暁人・藤井秀樹・黒田久泰・
新出尚之 の各氏に帰属します。** 本フォークは著作権を主張せず、また著作権の帰属や
利用条件を変更するものでもありません。原文は
[org-doc/copyrigh.txt](org-doc/copyrigh.txt) を参照してください。

### オリジナルのライセンス条件

明示されているのは次の点のみです。

- 著作権は作者4氏に帰属（`org-doc/copyrigh.txt`）
- **転載可** / ソフト種：フリーソフト（＝**無料**の意。自由に改変できるという意味
  ではありません）（[header.txt](org-doc/header.txt)）
- 本ソフトウェアを使用した結果生じた損害については一切保証しない

### 重要な注意点

**改変版の再配布は明示的に許諾されていません。** 「転載可」は慣行上、アーカイブを
無改変・完全な形で再配布してよいという意味で用いられ、改変版の配布を含みません。
本フォークは改変版であり、執筆時点で作者陣からの明示的な許諾を得ていません。

このため、**本リポジトリを MIT ライセンス等で再ライセンスすることはできません。**
オリジナルのライセンスはサブライセンス権を与えておらず、自分が持たない権利を
他者に許諾することはできないためです。MIT 化できるのは著作権者である4氏のみです。

サンプル曲（`sample/`）を同梱していないのも同じ理由です。各曲の著作権はそれぞれの
作曲者に帰属し、自由利用が明示されているのは門田氏作の曲のみです。同様に
`org-doc/coolgs.txt`・`coolxg.txt`・`coolsg.txt` も別の方々の著作物です。

作者陣から許諾が得られない場合、または公開の取り下げを求められた場合、本リポジトリは
非公開化します。オリジナルの配布物の説明は [readme.txt](org-doc/readme.txt) にあります。
