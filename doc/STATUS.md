# 変更履歴（STATUS）

本フォークの作業記録です。新しいものが上に来ます。
エンドユーザー向けの「オリジナル版との差分」は [CHANGES.md](CHANGES.md) にあります。

## 現状

| 項目 | 内容 |
| --- | --- |
| ベース | mml2mid Version 5.30b（門田暁人、藤井秀樹（MKR）、黒田久泰、新出尚之） |
| 入手元 | <https://www.vector.co.jp/soft/dl/unix/art/se102432.html> |
| 対応環境 | Windows / Linux / macOS（64bit） |
| ビルド | CMake（推奨）または `src/makefile` |
| 回帰テスト | トラック名 7/7・不具合修正/新機能 8/8・デバッグマップ 6/6・診断 3/3 合格（常に実行）。サンプル曲の回帰テストは非同梱のため既定では無効で、持ち込めば 33/33 合格（`ctest`） |
| バージョン表示 | `5.30`（`src/mml2mid.c` の `VER`、`CMakeLists.txt` の `VERSION`） |

検証済みツールチェーン:

| ツールチェーン | プラットフォーム | 結果 |
| --- | --- | --- |
| MSVC 19.51（VS 18） | Windows x64 | `/W3` で警告ゼロ、サンプル回帰テスト 33/33 合格 |
| GCC 10 | Debian x86-64 | `-O2 -Wall -Wextra` で警告ゼロ、サンプル回帰テスト 33/33 合格 |
| clang 21（clang-cl） | Windows x64 | エラー・実質的な警告なし |
| Emscripten 6.0.6 | WebAssembly (Node 24.19) | 警告ゼロ。SMF・デバッグマップともネイティブ版とバイト一致 |

生成MIDIは、決定的な32サンプル全てで上記ビルド間がバイト単位で一致します。


## オリジナル版のTODOリストにあった不具合の修正（3件）

`org-doc/todo.txt` の「以下のバグを直す．」に挙がっている6件を実機で検証し、
再現した4件のうち3件を修正した。該当コードは `.orig-euc/`（モダナイズ前の原本）と
一致していたので、いずれもモダナイズによる退行ではない。

| todo.txt の項目 | 状態 |
| --- | --- |
| `M1,50 c0e2` でモジュレーションがかからない | **修正** |
| `=1`〜`=0` をまたぐとテンポ設定が無効 | **修正** |
| 複数の `=0` でおかしくなる | 原文の例1〜3は再現せず。周辺で見つけた `ichi` のトラック間漏れを**修正** |
| 従属トラックが主トラックの複数行にまたがる繰り返しを追わない | 再現。未修正（下記） |
| `=0` をはさむと `^n` が休符になる | 再現。未修正（下記） |
| `a2&r2` ＋ Fコマンドでバグる（原文で「未確認」） | 再現せず |

修正の内容:

- **Mコマンド × 音長0の和音**（`src/note.c` の `emit_note()`）。音長0の音符は
  ステップを消費しないので `local.gatetime` が0になり、「ディレイがこの音符に
  収まるか」の判定（`mod_delay < local.gatetime`）が必ず外れて遅延モジュレーション
  が発行されなかった。加えて、続く和音構成音は `preandflag == 1` なのでMコマンドの
  処理自体が丸ごと飛ばされる、という二重の取りこぼしだった。
  音長0の場合だけ判定を次の音符へ持ち越す（`mod_chain_defer`）ことにした。
  `set_mmlproc()` に積んだ分は、音長0の音符で `emit_note()` が途中 return するため
  `mmlproc[]` に残り、次の音符の処理でそのまま書き出される。ステップが進んで
  いないのでディレイの値も補正不要。
  先頭が長さを持つ場合（`c&e2`）とディレイ超過時の非発行は従来どおり。

- **`=1`〜`=0` をまたぐテンポ**（`src/mmlproc.c` の `tmap_timing_comp()`）。
  テンポマップはトラック単位に順に積まれ、同じステップタイムでは `index`（＝積んだ
  順＝トラックの並び順）で順序が決まっていた。そのため「飛ばした区間のテンポ」が
  「先にコンパイルされた別トラックの `=0` 以後のテンポ」より後ろに来て、後勝ちで
  再開後のテンポが無効になっていた。`struct tmap` に、その指定を書いた時点で最後に
  通った `=` コマンドの行番号（`psw_line`）を持たせ、同じステップタイムでは
  `index` より優先して比較する。`=` は全トラックが同じ順に通るのでトラック間で
  比較でき、`=` が1つも無いMMLでは常に0なので順序は変わらない。
  差分指定（`TMAP_DIFF`）を普通の指定より後ろに置く既存の規則は温存している。

- **`ichi` のトラック間漏れ**（`src/mmlproc.c` の `converttrk()`）。「`=1` が
  あったかどうか」のフラグ `ichi` が `analyze()` で全体に一度しか初期化されて
  おらず、トラック毎の初期化（`psw = 0` の並び）に入っていなかった。行の中に
  `=1` を書いたまま終わったトラックのフラグが次のトラックへ持ち越され、`=1` を
  一度も通っていないトラックが `=0` の行でバンクセレクト＋プログラムチェンジ等を
  余計に出力していた。`ichi = 0` を追加。ついでに定義位置を `psw` の隣へ移した
  （`converttrk()` より後ろに定義されていたため）。

未修正の2件（いずれも構造的で、直すと既存MMLの解釈が変わりうる）:

- **従属トラックと複数行にまたがる繰り返し。** 繰り返しの内側の行に付けた従属
  トラックは、常に1周目の位置にだけ整列する。`src/charproc.c` の `gettrack()` に
  「ループ内ならその最初の時のステップに合わせちゃおう」というコメント付きの
  意図的な近似があり、作者自身が承知のうえで入れたもの。
- **`=0` をはさむと `^n` が休符になる。** `^` は音長パーサ（`length()`）が先読み
  して初めて音長の加算になる。この先読みは行をまたげるが、`=` 行は現トラックの
  文字列として返される（`charproc.c` の `gettrack()`）ため、先読みが `^` ではなく
  `=` に当たって音長確定が打ち切られ、取り残された `^` が `note('r')` になる。
  「行頭に置く `=`」と「行をまたぐ音長継続」の構造的な衝突。警告は出る。

また、`=1` 区間で設定した値のうち `=0` で復帰するのはバンク／プログラムチェンジ・
モジュレーション・ボリューム・パンポット・エクスプレッション・チャンネル
プレッシャーだけで、NRPN（`N`）・ディチューン（`D`）・ベンドレンジ（`BR`）・
ベンド（`BS`）・カットオフ／レゾナンス（`TC`/`TR`）は無言で捨てられることを確認した
（`org-doc/mml2mid.txt` は「全ての値」が反映されると書いている）。今回は未対応。

回帰テスト `test/regress/` を追加（後述）。修正前後のバイナリで、
`test/trackname/*.mml`・`org-doc/tr-rack.mml`・手書きの対照MMLを含む54ファイルを
コンパイルして出力をバイト比較し、差が出たのは上記3件の再現ケースだけであることを
確認済み。


## 不具合修正の回帰テスト（`test/regress/`）

生成MIDIのSHA-256を突き合わせる3つ目のテスト群。サンプル曲の回帰テストと同じ
`cmake/RunSampleTest.cmake` を使い、ベースラインは `test/regress.sha256`。
本フォークで書いたMMLなので、トラック名テストと同様に常に実行される。

| MML | 内容 |
| --- | --- |
| `mod-chord.mml` | Mコマンド × 音長0の和音。直った経路と、元から動いていた経路の両方 |
| `psw-tempo.mml` | `=1`〜`=0` をまたぐテンポ。todo.txt の例そのものと、その逆向き |
| `psw-ichi.mml` | 行中の `=1` が後続トラックに漏れないこと |
| `tempo-order.mml` | **対照**。`=` を使わない多トラック同時テンポ、相対指定・push/pop・`FT` の混在。上の修正で変わってはいけないもの |

`make check` からは `test/run-regress.sh` で実行する。


## サンプル曲の削除と非公式フォークであることの明示

`sample/` を削除。各曲の著作権はそれぞれの作曲者に帰属する第三者の著作物であり、
自由利用が明示されているのは門田氏作の曲のみのため、再配布しないことにした。

オリジナルのライセンス（`org-doc/copyrigh.txt` と `org-doc/header.txt`）で明示されている
のは「転載可」「フリーソフト＝無料」「無保証」のみで、**改変版の再配布は許諾されて
いない**。このためMIT等での再ライセンスもできない（サブライセンス権がないため）。
README にこの点と、本フォークが非公式・無認可であることを明記した。

付随する対応:

- サンプル曲が無い場合、CMake はその旨を表示してテストを無効化する（ビルドは正常）。
- `test/baseline.sha256` と `test/run-baseline.sh` は残置。オリジナル配布物の
  `sample/` をツリー直下に置けばそのまま検証できる。
- `test/README.md` に入手方法を記載。

**注意:** git履歴には削除前の `sample/` が残っているため、公開リポジトリから完全に
除去するには履歴の書き換え（`git filter-repo` 等）とforce pushが必要。

## 未リリース

### 統合環境向けの機能（2026-08-09）

MML統合開発環境 **[mml2mid64ide](../../mml2mid64ide)**（VS Code 拡張、別リポジトリ）の
バックエンドとして使うための3機能を追加した。要求仕様は同リポジトリの
`docs/BACKEND-REQUIREMENTS.md`、形式の正本は本リポジトリの
[doc/DEBUGMAP.md](DEBUGMAP.md)。

**MMLの解釈と生成MIDIは何も変わらない。** `-g` を付けない限り新しい出力は一切
生成されず、`-g` を付けても SMF は1バイトも変わらない（`test/dbgmap/` が毎回検証）。

#### 1. `-g` / `-g2` — デバッグマップの出力

MIDI tick と MML ソース行の対応表を `.mmlmap.json` として出力する。統合環境が
「再生位置に合わせてソース行をハイライトする」「カーソル行から再生する」ために使う。

実装は新規の `src/dbgmap.c` / `src/dbgmap.h`。設計上の勘所は3つ:

- **マップは後から導出できない。** SMFの出力はストリーム書き込み＋巻き戻し書き換え
  （`write_length()` が控えた `lastlenpos` へ `fsetpos2` で戻る）で、絶対tickを持った
  イベント配列がどこにも作られない。よってイベントを書き出すその場で記録する。
  巻き戻しの対象は常に「まだイベントが後続していない宙ぶらりんのデルタ」なので、
  イベント発行の時点で `tstep` を読めば確定した絶対tickになっている。
  `write_length()` にフックを置いてはならない。
- **行番号は `cur_line` ではなく新設の `cur_cmd_line` を使う。** `cur_line` は先読み
  （`length()` や `getbyte(1)`）が行末を越えると先へ進んでしまい、行の最後の音符が
  次の行のものとして記録されてしまう。`converttrk()` のディスパッチループで
  コマンドを取り出した直後に控える。`#include` の復元（`ppinfo`）も発行時には
  当てにできないので、ファイルも同じ時点で解決して `cur_cmd_file` に持つ。
- **`#include` の行・ファイル復元を `resolve_src_line()`（`charproc.c`）に括り出し**、
  `mml_err()` と共有した。二重に実装すると必ず食い違うため。

`struct tmap`（`src/mmlproc.h`）にソース位置の2フィールドを足した。既存の
`psw_line` は「最後に通った `=` の行」であってテンポ指定自体の行ではないため。

#### 2. `--diag=json` — 機械可読な診断

エラー・警告を1行のJSONとして stderr にも出す。統合環境が `vscode.Diagnostic` へ
変換するためのもの。人間向けのメッセージは何も変わらない。

長オプション（`--`）の受付を `options()`（`src/mml2mid.c`）の先頭に1箇所足した。
呼び出し側は先頭の `-` を落として渡してくるので、`-` で始まっていれば元の引数は
`--…` である、という判定で済む。

エラー回復は入れていない。コンパイラ全体が「初回エラーで `exit`」を前提に書かれて
おり、変更範囲が広く生成SMFを壊す危険があるため。**複数エラーの同時報告は
構造上できない**（警告は複数出る）。

#### 3. WebAssembly ビルドターゲット

`CMakeLists.txt` に `if(EMSCRIPTEN)` 分岐を追加した。統合環境に同梱して
インストール不要にするためのもの。ソースの変更は不要だった（`src/file.c` が既に
入出力を全てRAM上で扱う抽象化になっており、他に使っているのも Emscripten の
POSIX層にある関数だけのため）。VS Code の拡張機能ホストは Node なので、`NODEFS` で
実ワークスペースをマウントすれば `#include` もそのまま解決できる。

Emscripten 6.0.6 / Node 24.19.0 / CMake 4.3.3 + Ninja で検証済み。警告ゼロで
ビルドでき、**生成されるSMFもデバッグマップもネイティブ版(MSVC)とバイト単位で
一致する**（`test/` 配下の全24 MMLで確認）。手順とハマりどころは
[wasm/README.md](../wasm/README.md)。

#### テスト

| スイート | 件数 | 内容 |
| --- | --- | --- |
| `dbgmap.*` | 6 | `-g` 有無でSMFが一致すること＋マップが `.expected` と一致すること |
| `diag.*` | 3 | `--diag=json` のJSONが `.expected` と一致すること |

`test/dbgmap/` は音符列・`[ ]`ループ・`#include`・複数トラック・マクロ・テンポの
6ケース。マクロのケースは「本体の中身が呼び出し行に潰れる」という**仕様上の限界を
固定する**ためのもの。`ctest` 全体は 24/24 合格（サンプル曲を持ち込めば 49/49）。

### mmlpp のプリプロセス機能の統合（2026-08-09）

オリジナル配布物に同梱されている外部プリプロセッサ **mmlpp**
（[`mmlpp/mmlpp.pl`](../mmlpp/mmlpp.pl)、perl製、2001年以降未保守）の機能を本体に
取り込み、mmlpp 向けに書かれたMMLを `perl mmlpp.pl` を通さずにコンパイルできる
ようにした。仕様は [CHANGES.md](CHANGES.md#mmlの拡張) を参照。

これは `org-doc/todo.txt:22` の「`#define`コマンドをつける．（長いマクロ名＋変数）」
にあたる。[`mmlpp/mmlppdoc.txt`](../mmlpp/mmlppdoc.txt) 自身が「mml2mid本体で実現する
には大幅な改造が必要なので、実験的にプリプロセッサで実現してみました」と書いている
項目である。

**調査 — 統合作業は小さかった。** mmlpp.pl がやっている前処理のうち、注釈除去・
継続行の連結・行頭空白除去・`#include` の再帰展開・`# 行番号 "ファイル名"` マーカの
出力は、**すべて `getsp()`（[`src/mml2mid.c`](../src/mml2mid.c)）に実装済み**だった。
mmlpp.pl はそれを perl で再実装しているだけで、本体に無いのは次の3点だけだった。

| 機能 | mmlpp 側の実体 | 取り込み先 |
| --- | --- | --- |
| 長い名前のマクロ `${名前}` | `macroexp()` | 新規TU `src/macroexp.c` |
| 引数つきマクロ `${名前:引数}` / `#1` | `macroexp()` / `getarg()` | 同上 |
| ベンド値算出 `i` / `j` | `bendexp()` | `xget()`（`src/mmlproc.c`） |

**衝突の検討。**

- `$` の直後に `{` が来る記法は、拡張前は必ずエラーだった
  （[`src/charproc.c`](../src/charproc.c) の `getmacro()` / `track()` が
  `[0-9]?[a-z]` しか受け付けない）。新記法として安全に導入できる。
- `i` / `j` は `do_command()` の `switch` にも `escchr()` にも case が無く、
  コマンド文字として完全に未使用だった（`grep -n "case 'i'" src/` で0件）。数値位置に
  `i`/`j` が現れる既存MMLは拡張前も構文エラーなので、意味が変わる既存MMLは無い。
- 行中の `#` も未使用（`track()` が `default` で返し `do_command()` が構文エラーに
  する）。マクロ本体の `#1` と衝突しない。この事実は「旧方式の形の名前でも、
  引数を含む本体はマクロ展開段が引き取る」という判断の根拠にもなっている。
- `${名前}` の定義行は行頭が `$` なので、`gettrack()` と `build_track_map()` が
  既に読み飛ばす。トラック名の解釈には影響しない。

**方針 — 波括弧付きだけが新方式。** `${…}` は新しい展開段が処理し、`$a` / `$0a` は
`charproc.c` の既存機構のまま一切触らない。これにより既存MMLの生成MIDIが不変で
あることを保証できる（下記の検証）。この線引きの結果、mmlpp との差が1点だけ残る：
mmlpp は全マクロをテキスト展開してから mml2mid に渡すのでマクロ内の `[ ]` ループが
自己完結していなくてよかったが、本フォークでその挙動になるのは長い名前のマクロだけ
で、旧方式と同じ形の名前は従来どおり定義時にループを展開する。`${0a}` ≡ `$0a` と
いう mmlppdoc〔1.2〕の同一視を双方向で保つ方を優先した（そちらの方が、素の mml2mid
向けに書かれたMMLをそのまま通すという mmlpp 本来の目的に沿うため）。

**実装.**

- [`src/macroexp.c`](../src/macroexp.c) / [`src/macroexp.h`](../src/macroexp.h) を新設。
  `getsp()` が作ったクック済みイメージ(`fp1`)を入力に、`${…}` を展開した新しい
  イメージを返す `expand_macros()` だけを公開する。**1入力行＝1出力行**を維持する
  ので（マクロ本体は必ず1行に収まる）、行番号も `# 行番号 "ファイル名"` マーカも
  ずれない。呼び出しは `analyze()` の `getsp()` 直後・`scanmacro()` より前。
  - パス1で定義を集め（名前→本体のハッシュ表、再定義は後勝ち）、パス2で参照を
    展開する。2パスにしてあるので定義は使用より後ろでもよい。
  - 展開規則は mmlpp.pl の `macroexp()` / `getarg()` の移植。`\,` / `\\` の剥がし方、
    `{ }` グループの中の `,` を区切りと見なさず1段だけ `\` を剥がして波括弧ごと
    引数に含める挙動、引数の個数を検査しない挙動、文字列内では展開も `#n` の置換も
    しない挙動まで合わせてある。
  - mmlpp.pl は再帰マクロを書くと無限ループするが、こちらは深さ100で打ち切って
    エラーにする。
  - 未定義の名前が旧方式の形（`[0-9]?[a-z]`）で引数も無ければ、`$名前` を出力して
    `charproc.c` に委ねる。これで `${0a}` ≡ `$0a` が成り立つ。
  - 文字列の判定は `getLine_cooked()` と同じ規約（`"` と `'` の両方、内側の `\` と
    MS漢字1バイト目は次の1文字を読み飛ばす）に揃えた。
- [`src/mml2mid.c`](../src/mml2mid.c) — `prepro_error()` の中身を、`prepro_linebuf` に
  依存しない `prepro_msg_error(メッセージ, 行番号, ファイル名)` として切り出し、
  `macroexp.c` から使えるよう [`src/mml2mid.h`](../src/mml2mid.h) に宣言を出した
  （`prepro_msg_nomem()` も同様）。`prepro_error()` はそれを呼ぶだけの薄い関数に
  なった。
- [`src/mml2mid.c`](../src/mml2mid.c) — ディレクティブ `#bendrange <1〜24>` を追加。
  `getintdirective()` に `min` / `max` を足して、範囲外を**値を表示する前に**
  弾くようにした（既存の `#timebase` / `#xtempo` には `0, INT_MAX` を渡すので挙動は
  不変）。マクロ `${_bend_range_}` も同じ変数に反映するが、`#bendrange` があれば
  そちらを優先する。
- [`src/mmlproc.c`](../src/mmlproc.c) — `xget()` の `switch` に `case 'i': case 'j':`
  を追加し、`bend_semitone()` を新設。算出手順は mmlpp.pl の `bendexp()` と同じで、
  `64.0 * 半音数 / bend_range + 64.0` を **0〜127にクランプしてから** `+0.5` して
  切り捨てる（`j` はさらに `-64`）。符号は `i-2` のように文字の後ろに書く。
  `xget()` に入れたので数値を取るコマンドならどこでも使え、末尾の `+`/`-` 連結など
  既存の規則もそのまま効く。テキスト置換方式と違い、文字列内や非数値位置の `i`/`j`
  には一切触れない。
  `err_msgs[]` に82番 `"bend value 'i?' or 'j?' is wrong"` を追加。

確認したこと:

- **mmlpp.pl との等価性（end-to-end）。** `mmlpp/mmlppbnd.txt` 同梱のサンプルMML
  （`${GM_RESET}`・5引数の `${MIXER:…}`・`${_bend_range_} 8`・`~i2`・`~i-2`・
  `FB…j1`・`FB…j-8` を網羅）と、新設した回帰テスト3本を、
  「`perl mmlpp/mmlpp.pl` を通してからコンパイル」と「直接コンパイル」の2通りで
  比較し、**4件すべて生成MIDIがバイト単位で一致**した。
- **既存出力の不変性。** ローカルにあるオリジナル配布物のサンプル32曲を、
  本作業の前（`2a960a4`）の実行ファイルと本作業後の実行ファイルでコンパイルし、
  **32/32 でSHA-256が一致**した。`ctest` も 15/15 合格（対照ケース
  `tempo-order.mml` を含む既存4件のハッシュは不変）。
- エラー経路。未定義マクロ、`}` が無い引数リスト、自己再帰、空のマクロ名、
  `${ab` のような壊れた参照、`#bendrange 0` / `25`、数字を伴わない `~i` が、いずれも
  行番号（`#include` した先で起きた場合はそのファイル名も）付きで停止し、終了
  コード1で出力ファイルを残さないこと。
- `#include` したファイルの中で定義したマクロが、インクルード元でも使えること
  （mmlpp が `#include` の展開まで自前でやらなければならなかった理由がこれ）。
- `#bendrange 8` と `${_bend_range_} 2` を同時に書いた場合、生成される
  ピッチベンド値が `48 50 60 7f 00 40`（＝レンジ8で計算した 72/80/96/127/0/64）に
  なり、`#bendrange` が優先されること。
- MSVC 19.51 でビルドし、`/W3` で警告ゼロ。`/W4` でも `macroexp.c` 由来の警告は
  出ない（全TU共通の `compat.h` の `strncat` 警告のみ）。

**未対応。** `BW` 用のベンド値（0〜16383）を算出する機能は mmlpp にも無く、今回も
入れていない。

**別件の発見 — `test/baseline.sha256` が4曲分だけ古い（今回の変更とは無関係）。**
上のサンプル32曲の照合で、`test/baseline.sha256` と食い違う4曲
（`01mkr29`, `01mkr39`, `01mkr40`, `02nm63`）があることに気付いた。追跡した結果、
**本フォークが意図して入れた挙動変更を反映していないだけ**で、退行ではなかった。
`.mml` の文字コード（全曲EUC-JP）や改行（全曲LF。CRLFに変換しても出力は変わらない）
は原因ではない。

| 曲 | 原因 | 差 |
| --- | --- | --- |
| `02nm63` | **`A1A` の従属トラック取りこぼしの修正**（トラック名2文字化の副作用。「本フォークで唯一、生成MIDIが変わりうる変更」として下記に記載済み） | `.mml` 46行目が `ZA1ACJBDMN…` と、主トラック `A` より後ろに従属トラック `1A` を書いている。オリジナル版はこれを黙って捨てていた。トラック数が 28 → 29 |
| `01mkr29` `01mkr39` `01mkr40` | **`M`コマンド × 音長0の和音の修正**（`2a960a4`、下記「オリジナル版のTODOリストにあった不具合の修正」） | 遅延モジュレーションが正しく発行されるようになった分だけ増える（+159 / +2094 / +525 バイト） |

裏付け:

- `2a960a4` の親（`30932dd`、TODO由来の不具合修正の直前）でビルドすると、
  `01mkr29` `01mkr39` `01mkr40` を含む **31/32 が `test/baseline.sha256` と一致**し、
  残る `02nm63` だけが食い違う。`02nm63` はトラック名2文字化の時点で既に変わっていた。
- その `30932dd` のビルドが出す `01mkr29` `01mkr39` `01mkr40` のファイルサイズは、
  同梱の `.mid`（オリジナル版が生成したもの）と完全に同じ（31217 / 88980 / 8314）。
- 同梱 `.mid` との照合（Shift-JISへ変換して `-m` でコンパイル）では、上の4曲と
  `00master`（乱数）・`01hop2gs`・`10kazeir`（テンポ値の丸め、各1バイト）以外の
  **27曲がバイト単位で完全一致**する。

つまり `test/baseline.sha256` は、トラック名2文字化とTODO由来の不具合修正のあとに
再生成されていなかった。サンプル曲は非同梱でこのテストが既定で無効なため表面化して
いなかったが、`sample/` を持ち込むとこの4曲が不合格になる状態だった。

対応として、`test/README.md` の手順どおり**この4曲分のベースラインを再生成**した
（差分は4行のみで、他の28曲のハッシュは不変であることを確認済み）。あわせて
「32件中28件が同梱 `.mid` と一致」という記述を、モダナイズ時点の記録であることを
明示したうえで現状（25件）に更新し、意図して変えた4曲の内訳と追跡の根拠を
`test/README.md` に「Samples this fork changed on purpose」として書き足した。

### 1トラック和音演奏の拡張 — 個別ノートオフと`r0`（2026-08-06）

オリジナル版にすでにある「音長0の音符はノートオンだけ生成してステップを進めない」
機能（`c0e0g4.` で1トラックに和音を書ける、Ver5.09で追加・`org-doc/mml2mid.txt`に
記載）を使って、和音の一部だけを個別にノートオフできるようにしたいという要望が
あった。仕様は [CHANGES.md](CHANGES.md#mmlの拡張) を参照。

**衝突の検討。** 実装前に、既存の文法との衝突を調査した。

- バッククォート `` ` ``(0x60) は、字句レベル（`src/charproc.c` の `track()` /
  `macro()` トークナイザが特殊扱いするのは `[ ] / : $ " スペース タブ |` のみ）
  でもコマンドレベル（`do_command()` のswitchに該当ケースが無く、現状は
  `default: syn_err` に落ちる。`escchr()` にも未登録）でも完全に未使用だった
  （`grep -rn '\`' src/` で確認）。新しいプレフィックスとして安全に導入できる。
- 音名(`a`〜`h`)・`l` コマンドの音長0は、要求された時点で**既に実装済み**
  だった（[note.c](../src/note.c) の「音長ゼロの音符」ブロック、Ver5.09で
  `l0`/`l%0` も許可済み）。休符(`r`)だけが例外で、`r0` は
  `if((local.oto & OTO_KYUFU) && local.oncho == 0) return;` により常に
  無視される仕様だった。これは Ver5.02 で「音長ゼロの休符があると変な動作を
  するのを修正した」というバグ修正由来のガードで
  （`org-doc/chnglog.txt`）、`r0`を「発音中の和音をノートオフする」に
  変えるのは、この無視という仕様そのものを変更する後方非互換な変更になる。
  サンプルMML(`.orig-sample/`)に `r0` の実使用は見つからなかった。
- 「発音中のノート」をエンジンが一元的に持つレジストリは無く、実体は
  `keyproc[]`（音長0や`&`で繋いだ、次の非タイ音符/休符でまとめてノートオフ
  される音の配列）と `keyproc2[]`（負のゲートタイムで将来ノートオフ予定の
  音の配列、別機能）の2つのみ。和音記法が使っているのは前者なので、
  `r0` や `` `<音名> `` の対象は `keyproc[]` に限定することにした
  （ユーザーに確認済み）。
- `` `<音名> `` で音長を省略した場合のデフォルトを検討した。既存の全コマンド
  （音名・`r`・`l`）は「省略時は`l`コマンドの現在値を採用」という規約なので、
  ここでもそれに合わせることにした（ユーザーに確認済み）。「省略時は即座に
  ノートオフ」という特別扱いは既存の規約と矛盾するため採用しなかった。

**実装** ([`src/note.c`](../src/note.c), [`src/mmlproc.c`](../src/mmlproc.c)):

- `get_note_values()` 内の音階計算（オクターブ・臨時記号・移調・強制転調）を
  `compute_onkai()` として切り出した。`` `<音名> `` からも同じ計算を使うため。
- `r0` のガードを `keyproc_ptr == 0` の場合だけ無視するように変更。
  `keyproc_ptr != 0` のときは既存の「音長ゼロの音符」ショートカットを
  素通りして（このブロックに休符除外の条件を追加）、下のキーオフ処理まで
  そのまま進む。休符は `local.gatetime` が常に0になる既存の規約により、
  そこは「即時キーオフ」分岐（`keyproc[]`を全件ノートオフする既存の経路）に
  自然に入る。新規ロジックはほぼ無く、ガード条件2箇所の変更のみ。
- `note()` を薄いラッパーにし、実体を `note_impl(code, no_flush)` に分離。
  `` `r `` 用の `note_r_noflush()` は `note_impl('r', 1)` を呼ぶだけ。
  キーオフ処理セクション冒頭の条件を
  `if(!(local.oto & OTO_KYUFU) || (keyproc_ptr != 0 && !no_flush))` に変更し、
  `no_flush` が真のときは強制的に「単なる休符」分岐（ステップを進めるだけで
  ノートオフを一切しない、既存の経路）に入るようにした。F/I/M・
  `keyproc2`・`psw`・ランニングステータス等はすべて既存のまま自動的に効く。
- `` `<音名> `` 用に `note_off_specific()` を新設。`compute_onkai()` で
  対象ピッチを求め、`length()` で音長を解決し（省略時は`l`値、既存と同じ
  規約）、音長分だけ「単なる休符」と同じ手順（`tstep`/`lastlenpos` の
  巻き戻しトリック→ステップ0で確定しているmmlprocだけ即時消化→
  `write_length`）でステップを進めてから、`keyproc[]` を先頭から線形探索して
  一致するエントリを1つだけ `noteoff()` し、`shift_array` マクロ（既存）で
  配列から除去する。一致が無ければ新エラー75番。`andflag`/`preandflag` には
  触れない（和音構築中のタイ連鎖の状態を乱さないため）。
- `do_command()` の switch に `` ` `` を追加。`case 'h':` と同じ流儀で次の
  1文字を読み、`a`〜`g`・(german_scaleなら)`h` は `note_off_specific()`、
  `r` は `note_r_noflush()` に振り分ける。無効な文字は実際に問題の文字を
  `code` に代入してから既存の `syn_err` に合流させる。
- `err_msgs[]` に75番
  `"backquote note off '`?' has no matching sounding note"` を追加。

**既知の限度。** トラック終端で `keyproc[]` に閉じられていないエントリが
残っていても、現状は何もノートオフされない（`write_keyproc2()` /
`write_restofmmlproc()` は `keyproc2[]` / `mmlproc[]` しか見ていない）。
これは今回の変更以前から存在する挙動で、今回のスコープでは修正していない。

確認したこと:

- 手コンパイルしたMMLのバイト列を `od -An -tx1` で確認。
  - `c0e0g4.`（既存機能）… 変更前と同じ和音（c,e,g同時発音、`g4.`終了時に
    3音まとめてノートオフ）になること。
  - `c0e0 `e g4` … `e` だけ48ステップ後（省略時は`l4`値）に単独で
    ノートオフされ、残った `c` が `g4` 自身のキーオフと一緒にノートオフ
    されること。
  - `c0e0 `e4 g4` … 明示的な`4`（`l4`と同値）が省略時と同一バイト列になる
    こと。
  - `c4 r0` … 何も起きない（`keyproc_ptr == 0` のため、48ステップの`c4`
    以外に追加のイベントが無い）こと。
  - `c0e0 r0` … `c`,`e` がその場でまとめてノートオフされ、トラック全体の
    ステップ数が0になること。
  - `c0e0 `r4 `c `e` … `` `r4 `` はイベントを書かずにステップだけ進め
    （次のイベントとのデルタタイムに正しく合算される: 96 = 48(`` `r4 ``) +
    48(`` `c `` 省略時)）、`` `c ``・`` `e `` がそれぞれ単独でノートオフ
    されること。
  - `` `x ``（無効な文字）… 実際の文字 `x` を表示して構文エラーで停止する
    こと。
  - `` `c `` 単体（対応する発音中の音が無い）… 新エラー75番で停止すること。
- `MSVC 19.51`（`/W3`）でビルドし、新規警告が無いこと。
- `ctest -C Release` が 7/7 合格すること（サンプル曲は非同梱のため未実行）。

### `Y` コマンドの追加（2026-08-06）

未使用だった英大文字 `Y` に、モノモード切替の専用コマンドを割り当てた。仕様は
[CHANGES.md](CHANGES.md#mmlの拡張) を参照。

- `Yn`（`n`=0〜127、省略時1）でコントロールチェンジ126番（Mono Mode On） `n` を
  送出する。ただし `n=0` の場合だけ例外的にコントロールチェンジ127番
  （Poly Mode On） `0` を送出する（モノモード解除は127番を送るのがGM/GS等の作法
  のため）。
- 既存の `y`（小文字）コマンドは任意のコントロールチェンジを2引数で直書きする
  汎用コマンドで、これを使えば `y126,1` / `y127,0` として同じ結果を得られる。
  `Y` はこの2つの組み合わせのショートハンド。

**衝突の検討。** `do_command()` の `switch` に大文字 `Y` を処理する `case` は無く、
`Y` は他のどのコマンドにも使われていなかった（`grep -rn "'Y'" src/` で確認）。
トラック名の1文字目としては `A`〜`Z` すべてが使えるが、これは行頭でのトラック
選択という別の文法要素であり、`do_command()` はトラックのMML本体を読んでいる間
だけ呼ばれるので衝突しない（`H`, `M`, `D` など他の大文字コマンドと同じ関係）。

実装（[`src/mmlproc.c`](../src/mmlproc.c)）:

- `do_command()` に `case 'Y': code_Y(); break;` を追加。
- `code_Y()` を新設。`xget()` で引数を読み、省略時（`i == -2`）は1を補う。
  0〜127の範囲外は新エラー74番。`n == 0` なら `put_cntchange(127, 0, 0)`、
  それ以外は `put_cntchange(126, num, 0)` を呼ぶ。`P`/`X` コマンドの `pedal()` と
  同じ構成。
- `err_msgs[]` に74番 `"mono mode 'Y?' is wrong"` を追加。

確認したこと:

- `Y`（引数省略）が `b0 7e 01`（CC126, 1）を出力すること。
- `Y0` が `b0 7f 00`（CC127, 0）を出力すること。
- `Y64` が `b0 7e 40`、`Y127` が `b0 7e 7f` を出力すること。
- `Y128` が新エラー74番で停止すること（終了コード1）。
- `ctest -C Release` が 7/7 合格すること（サンプル曲は非同梱のため未実行）。

### `@(msb,lsb,prog)` コマンドの追加（2026-08-06）

`@` にバンクセレクト付きプログラムチェンジの括弧記法を追加した。仕様は
[CHANGES.md](CHANGES.md#mmlの拡張) を参照。

**衝突・後方互換性の検討。** `@` の後続文字を決める `xget()` は、認識できない
文字（`(` を含む）に遭遇すると、その文字を消費せずに `i = -2` を返す
（[`src/mmlproc.c`](../src/mmlproc.c) の `xget()` の `default:` 節）。
このため `@(` は拡張前から `mml_err(23)` で止まる文法エラーであり、この拡張が
意味を変える既存のMMLは存在しない。`(` / `)` はトップレベルではベロシティ
昇降コマンド（`case '(': kakko2();` / `case ')': kakko1();`）だが、これは
`do_command()` が新しいコマンド文字を読む瞬間にのみ関係する。`t(` / `t)`
（テンポスタックの push/pop）が既に同じ手法で `(` / `)` を先読み・消費してから
サブ文法に入っており、`progchange()` 内で `getbyte(1)` により `(` を先読みして
消費すれば、外側のディスパッチループに `(` が渡ることはなく衝突しない。

実装（[`src/mmlproc.c`](../src/mmlproc.c)）:

- `progchange()` の先頭で `getbyte(1) == '('` を先読み。真なら `xget()` →
  `,` → `xget()` → `,` → `xget()` → `)` の順にMSB・LSB・プログラム番号を読み、
  誤りがあれば新エラー73番で停止する。引数の解析はすべて `xget()` に任せている
  ので、変数・16進・加減算の記法がそのまま使える。
- バンクセレクトは既存の `H` コマンド（`bankselect()`）と同じ `bs1` / `bs2` を
  更新し、`put_cntchange0(0, bs1)` / `put_cntchange0(32, bs2)` で送出する。これに
  より、`=` スイッチ（`setpsw()`）がプログラムチェンジ再送時に読む `bs1` / `bs2`
  とも整合する。
- プログラムチェンジ本体の送出は `put_progchange()` として切り出し、従来の
  `@n` 経路と `@(...)` 経路の両方から呼ぶ（重複コード排除）。
- `prog` には従来どおり `base`（`@+n` / `@-n` で設定）を加算するので、
  `@(msb,lsb,prog)` の `prog` にもオフセットが効く。
- `err_msgs[]` に73番 `"program change '@(?,?,?)' is wrong"` を追加。

確認したこと:

- `@(1,2,5)` と `H1,2 @5` をそれぞれコンパイルし、生成されるバイト列
  （`b0 00 01 00 20 02 00 c0 05`）が完全一致することを確認。
- `@+3 @(1,2,2)` が `@+3 @2` と同じ `c0 05` になり、`base` オフセットが括弧内の
  `prog` にも適用されることを確認。
- 引数不足（`@(1,2)`）・閉じ括弧なし（`@(1,2,3`）がいずれも新エラー73番で
  停止すること（終了コード1）を確認。
- `ctest -C Release` が 7/7 合格すること（サンプル曲は非同梱のため未実行）。
  従来の `@n` / `@+n` / `@-n` の経路は分岐前と同一のコードのままなので、
  既存サンプルの生成MIDIへの影響はない。

### トラック名の2文字化（2026-08-06）

トラック名を `[A-Z]` から `[A-Z][0-9]?` に拡張し、主トラックを26本から286本に
増やした。仕様は [CHANGES.md](CHANGES.md#トラック名--2文字目に数字を書けるようになりました)
を参照。

**2文字目を数字に限った理由。** 当初の案は `[A-Z][0-9A-Z]` だったが、これは
オリジナル版の並列表記（`AE` ＝ トラック `A` と `E`）と衝突して解決できない。
2文字目を数字に限ると、衝突するのは「英大文字＋数字」と「英大文字＋従属トラック
番号＋英大文字」の間だけになり、次の1文字を先読みすれば区別できる。

**曖昧性の解決規則.** 数字（あるいは `?`）を2文字目と見なすのは、その次の文字が
英大文字でも `?` でもない場合だけ。そうでなければ次のトラック指定の先頭と見なす。
この規則により `A1B`（＝ `0A`, `1B`）や `?1?`（＝ `0A`-`0Z`, `1A`-`1Z`）といった
オリジナル版の並列表記の意味が一つも変わらない。

実装:

- [`src/charproc.h`](../src/charproc.h) — トラック名を通し番号 `talf` に符号化する
  マクロ群を新設。`talf = (1文字目 - 'A') * TRK_SUFFIXES + 2文字目コード`、
  2文字目コードは無しが0で `'0'`〜`'9'` が1〜10。`talf == 0` が従来どおり
  トラック `A` になるので、`talf` を0から回す既存のループがそのまま使える。
- [`src/charproc.c`](../src/charproc.c) — `getsuffix()` を新設（2文字目の読み取りと
  上記の先読み）。`gettrack()` はこれを呼び、`talf` を1文字目・2文字目に分解して
  照合するようにした。あわせて、一致しなかった `?` で行の残りの並列指定を捨てて
  しまわないよう、内側ループの脱出条件を `!is_upper(i2)` から
  `!(is_upper(i2) || i2 == '?')` に変更（`?` は2文字目のせいで一致しないことが
  あるため。1文字名しかなかった頃は `?` は必ず一致していたので影響はない）。
- [`src/charproc.c`](../src/charproc.c) — トラックマップの作成を、`gettrack2()` に
  よる「トラック名1個につきファイル全走査」から、`build_track_map()` による
  **1回の走査**に置き換えた。トラック名が26通りから286通りに増えるため、
  従来方式では走査回数が11倍になる。`analyze()` から明示的に呼ぶ。
- [`src/mml2mid.c`](../src/mml2mid.c) — `track_map[]` を `TRKMAP_SIZE` に拡大
  （添字の刻みを32から `TRKMAP_STRIDE` へ）。`talf` のループを `NTRKNAME` まで回す。
- [`src/mmlproc.c`](../src/mmlproc.c) — 進捗表示の `trk %d%c` を
  `trk %d%s` + `trkname_str()` に変更。

**トラック数の上限**（`MAXTRKNUM`）を64から `10 * NTRKNAME + 1`（2861）にした。
あわせて `smfheader()` がSMFヘッダのトラック数を1バイトでしか書いていなかったのを
16ビットで書くように修正（従来は上位バイトに0を書きっぱなしだった）。64本以下では
出力バイト列は変わらない。`format1to0()` の `ctrk[]` / `endsw[]` は要素数が増えた
ので `static` にしてスタックから外した。

**副作用の不具合修正。** トラックマップを1回の走査で作るようにしたことで、
オリジナル版の遅延作成が持っていたバグが直った。従来は、主トラックを見つけた時点で
その行の残りを読むのをやめていたため、`A1A` のように主トラックより後ろに書いた
従属トラックがマップに載らず、黙って捨てられていた（`1AA` と書けば正しく動いた）。
**本フォークで唯一、生成MIDIが変わりうる変更**なので [CHANGES.md](CHANGES.md) にも
記載した。

確認したこと:

- HEAD（`56440c4`）の実行ファイルを別途ビルドし、オリジナル版の記法だけを使った
  MML 7本（`org-doc/tr-rack.mml` を含む。並列表記・ワイルドカード・従属トラック・
  マクロ・ループ・`EX`・`#title`/`#timebase` を網羅）を、オプション8通り
  （なし / `-f` / `-t3` / `-c` / `-x` / `-v` / `-w` / `-m`）でコンパイルして比較。
  56件すべてで標準出力・終了コード・生成MIDIのバイト列が一致。
- 新記法（`A0`, `Z9`, `1A0`, `A?`, `?0`, `??`, `A00B1`, `A10B`, `A0B1`）が上記の
  規則どおりに解釈されることを、進捗表示のトラック名で確認。
- 全2860トラック＋テンポマップ（＝ `MAXTRKNUM` ちょうど）のMMLをコンパイルし、
  SMFヘッダのトラック数が 2861 になり `MTrk` チャンクも 2861 個あることを確認。
- サンプル曲が無いため `ctest` のサンプル回帰テストは未実行。上記の同一性確認で
  代替した。`sample/` を持ち込める環境では別途確認が必要。

トラック名の文法を固定する `ctest` のテストスイートを新設した
（[`test/trackname/`](../test/trackname/)、[`cmake/RunTrackNameTest.cmake`](../cmake/RunTrackNameTest.cmake)）。
本フォークで書いたMMLなので第三者の著作物を含まず、サンプル曲と違って常に実行できる。
`.mml` ごとに、出力されるべきトラック名を出力順に並べた `.trk` を置き、標準エラーの
`trk 0A0: ...` と照合する。SMFヘッダのトラック数も検査する。

| テスト | 内容 | オリジナル版での結果 |
| --- | --- | --- |
| `parallel` | 並列表記（`AE`, `A1B`, `1A2B`, `1BC2D`, `0A1BC2D`） | 合格 |
| `wildcompat` | 従来のワイルドカード（`?`, `1?`, `?1?`） | 合格 |
| `subafter` | 主トラックより後ろの従属指定（`A1A`, `0?2?`） | 不合格（上記の不具合） |
| `twochar` | 2文字のトラック名（`A0`, `A1`, `Z9`, `1A0`, `9Z9`） | 不合格 |
| `lookahead` | 2文字目の切り分け（`A1B`, `A0B1`, `A10B`, `A00B1`） | 不合格 |
| `wildcard` | 桁ごとのワイルドカード（`A?`, `?0`） | 不合格 |
| `manytracks` | `?` + `??` で286トラック（トラック数が1バイトを超える） | 不合格 |

「オリジナル版での結果」は、HEADの実行ファイルに対して同じテストを流して確認した。
互換性のみを見る2件が合格し、残り5件が意図した理由で不合格になる。

### ドキュメントの再編（2026-08-06）

- オリジナル配布物の `doc/` を **`org-doc/`** にリネーム。内容は一切変更せず、
  オリジナルのドキュメントの置き場とする。
- 本フォークで書いたドキュメントを新しい `doc/` 配下に Markdown で置く方針とし、
  [`doc/STATUS.md`](STATUS.md)（このファイル）と [`doc/CHANGES.md`](CHANGES.md) を新設。
- `README.md` / `BUILD.md` / `.gitattributes` の `doc/` への参照を更新。

`org-doc/` は原則として編集しない。仕様変更の記述は `doc/CHANGES.md` 側に書く。
このため `P` / `X` の拡張（下記）も `org-doc/mml2mid.txt` と `org-doc/command.txt`
には反映していない。

### `P` / `X` コマンドの拡張（2026-08-06）

`P` / `X` が 0〜4 の引数を取れるようにし、ダンパー・ペダル以外のペダル系
コントロールチェンジ（65〜68番）にも対応した。仕様は
[CHANGES.md](CHANGES.md#mmlの拡張) を参照。

実装（[`src/mmlproc.c`](../src/mmlproc.c)）:

- `pedal(int on)` を新設。引数を `xget()` で取り（省略時は0）、範囲外なら新エラー
  72番でエラーとし、`put_cntchange(64 + n, on, 0)` を出力する。
  引数の解析を `xget()` に任せているので、変数・16進・加減算の記法がそのまま使える。
- `mmlcmd()` の `case 'P'` / `case 'X'` を `pedal(127)` / `pedal(0)` に置き換え。
- `err_msgs[]` に 72番 `"pedal 'P?' or 'X?' is wrong"` を追加。

確認したこと:

- `P`, `X`, `P0`〜`P4`, `X0`〜`X4` の12通りをコンパイルし、出力が
  CC64=127 / CC64=0 / …… / CC68=127 / CC68=0 になることをバイト列で確認。
- `P5` と `X-1` が新エラーで停止すること（終了コード1）。
- `ctest -C Release` が 33/33 合格すること。引数なしの `P` / `X` は従来どおり
  CC64 なので、既存サンプルの生成MIDIは不変。

## 2026-08-06 — 初期モダナイズ

オリジナルの mml2mid 5.30b を64bit環境でビルドできるようにした一連の作業。
生成MIDIの内容は変えていない（根拠は [test/README.md](../test/README.md)）。
技術的な詳細は [BUILD.md](../BUILD.md) にある。

### `a46ca58` — ドキュメントをEUC-JPからUTF-8に変換

ドキュメント（`*.txt`、`src/mml2mid.1`）を文字列を1文字も変えずにUTF-8へ変換。
`sample/*.mml` と `org-doc/tr-rack.mml` はコンパイラへの入力データなので
EUC-JPのまま据え置き（変換すると生成MIDIのバイト列が変わるため）。

### `3385905` — フォークであることの明示

`README.md` と `BUILD.md` に、本ツリーが mml2mid 5.30b のフォークである旨、
オリジナル作者、入手元を記載。

### `2841323` — 初期コミット（64bit化）

ビルドシステム:

- `CMakeLists.txt`（MSVC / GCC / Clang対応）を追加。`src/makefile` を
  `install` / `check` ターゲットを持つ移植性のあるMakefileに書き直し。
- `-DUNIX` を廃止し、`_WIN32` の有無でプラットフォームを判別。
- 旧来の `makefile.bcc` / `makefile.egc` / `makefile.lcc` および `file.asm`
  （16bit DOS用アセンブラ）はどのビルドからも参照されない。

64bit対応:

- `Fpos_t` が `long` だった。ファイル位置はポインタ同士の差から計算されるため、
  `long` が32bitであるWin64では算出された位置が毎回切り詰められていた。
  `ptrdiff_t` に変更し、`fseek2` / `ftell2` / `smftrkend` も追随。
- `qsort()` にプロトタイプなし関数ポインタ型へキャストした比較関数を渡していた
  （未定義動作）。正しい `(const void *, const void *)` 署名に修正。

移植性レイヤ:

- `win.h` を `compat.h` に置き換え、`UNIX` / `WINDOWS` / `MSDOS` / `BCC` /
  `LSI_C` の手動切り替えをWin32とPOSIXの実際の分岐に整理。feature-testマクロを
  設定するため、各 `.c` の最初の `#include` である必要がある。
- DOS用ファーポインタ（LSI-C）版とBorland C版のメモリ上ファイル層、Win16 GUI用の
  エントリポイント（`mml_smf`、`setjmp`/`longjmp` によるエラー処理、`hWnd3`、
  `InvalidateRect`/`UpdateWindow` のダミー）を削除。
- `isascii()`（標準Cではなく、MSVCには存在しない）を、`EOF` を正しく偽と判定する
  明示的な範囲チェックに置き換え。
- `read()` / `write()` が短い転送と `EINTR` をループするように修正
  （`<errno.h>` 未インクルードのため `EINTR` の再試行が無効化されていた）。
- パイプからの読み込みに対応（`fdopen2` が `fstat` のサイズ報告に依存しなくなった）。

修正した不具合:

- **`EX` / `EE` でのスタックバッファオーバーフロー** — `getexclusive()` が
  `int exclusive[1024]` に境界チェックなしで書き込んでいた。新エラー
  71番 `"exclusive data too long"` を追加。
- **乱数ベロシティでのゼロ除算** — `kr1`（または `kr n,0`）で除数が0になった。
- **`text[]` のオーバーフロー** — 8KB固定バッファへ `strcat` で追記しており、
  `S` コマンドの多いトラックで溢れた。`text_cat()` に集約し、`Msg[]` への
  `sprintf` / `wsprintf` はすべて `snprintf` に変更。
- **タイトル/著作権表示のoff-by-one** — 長さは1バイトで書き出されるのに256を許して
  おり、長さ0として書き出されていた。上限を255に修正。
- **早期エラー時のヌル参照** — `free_all_macros()` がエラー経路から呼ばれ、
  `init_all_macros()` の実行前に `mcrstr` を参照していた。
- **`realloc` のリーク** — `reallocmacro()` と `getLine()` で、結果をブロックへの
  唯一のポインタへ代入していた。
- `put_cpres` の宣言が `static` で定義が非staticだった。
- `setcode_I()` の死んだ状態 `dir[]`（書くだけで読まれない）を削除。

テスト:

- `sample/*.mml` をすべてコンパイルしてSHA-256を照合する回帰テスト
  （`test/baseline.sha256`、`ctest`、`make check`）を追加。
- ベースラインは同梱の `.mid`（オリジナル版が生成したもの）と照合して検証済み。
  32件中28件がバイト単位で完全一致。残り4件の原因は [test/README.md](../test/README.md)。
  （この時点の記録。その後の意図した挙動変更に合わせて、2026-08-09 に4曲分の
  ベースラインを再生成している。上記の「mmlpp のプリプロセス機能の統合」を参照）
